# HTMLCUT upstream provenance

This directory stores HTMLCUT release references and intake notes.
Actual Tauri bundle inputs live under `src-tauri/binaries/`.

Rules:

- do not store bundled sidecar binaries here
- keep only provenance material that helps review exact upstream intake
- do not treat this directory as proof of a shipped release until exact artifacts and checksums are recorded
